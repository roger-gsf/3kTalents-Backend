// Create a interface called "Products" that has the attributes "type, price, name, weight";
// Create 3 classes that utilize that interface;
// Create a different method for each product;
// Instantiate the classes in the "index.ts" and call this methods and attributes.

export interface Products {
    _name: string;
    type: string;
    price: number;
    weight: number;
}